<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ipvc.estg.cmtp1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cmtp1">
        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/button" />

        <activity android:name=".MainActivity" />
        <activity android:name=".notaEdit" />
        <activity
            android:name=".editNota"
            android:label="@string/title_activity_edit_nota"
            android:theme="@style/Theme.Cmtp1.NoActionBar" />
        <activity android:name=".addNota" />
        <activity android:name=".begin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>